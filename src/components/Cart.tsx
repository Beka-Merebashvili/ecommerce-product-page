import styled from "styled-components";

const Cart = () => {
    return (
        <StyledDiv>

        </StyledDiv>
    )
 }

 export default Cart;

 const StyledDiv = styled.div`
 width: 220px;
 height: 130px;
 background-color: red;
 position: absolute;
 bottom: -100px;
 z-index: 2;
 `